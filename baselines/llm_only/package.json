{
  "name": "@morphapi/baseline-llm-only",
  "version": "0.1.0",
  "private": true,
  "description": "Pure LLM MorphPay migration baseline (expects scaffolding hallucination / typecheck fail)",
  "scripts": {
    "start": "tsc -p tsconfig.run.json && node dist-run/run.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@morphapi/core": "*",
    "morphpay-v2": "*"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
